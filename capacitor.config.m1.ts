import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vocab.m1',
  appName: 'Vocab M1',
  webDir: 'vocab_by_grade/M1',
  android: { path: 'android-M1' },
  ios: { path: 'ios-M1' }
};

export default config;
